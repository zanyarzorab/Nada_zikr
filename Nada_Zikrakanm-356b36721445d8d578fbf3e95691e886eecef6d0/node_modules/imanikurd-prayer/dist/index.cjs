"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var index_exports = {};
__export(index_exports, {
  KAABA_COORDINATES: () => KAABA_COORDINATES,
  KURDISTAN_CITIES: () => KURDISTAN_CITIES,
  calculateQibla: () => calculateQibla,
  clearDataCache: () => clearDataCache,
  getCity: () => getCity,
  getCityByCoordinates: () => getCityByCoordinates,
  getCityDisplayName: () => getCityDisplayName,
  getCityFromCoordinates: () => getCityFromCoordinates,
  getCityName: () => getCityName,
  getDailyPrayerRecord: () => getDailyPrayerRecord,
  getDailyPrayerRecordAsync: () => getDailyPrayerRecordAsync,
  getDataFilePath: () => getDataFilePath,
  getDistanceToKaaba: () => getDistanceToKaaba,
  getKaabaCoordinates: () => getKaabaCoordinates,
  getMonthlyPrayerTimes: () => getMonthlyPrayerTimes,
  getMonthlyPrayerTimesAsync: () => getMonthlyPrayerTimesAsync,
  getNextPrayer: () => getNextPrayer,
  getNextPrayerAsync: () => getNextPrayerAsync,
  getPrayerCities: () => getPrayerCities,
  getPrayerCitiesAsync: () => getPrayerCitiesAsync,
  getPrayerTimes: () => getPrayerTimes,
  getPrayerTimesAsync: () => getPrayerTimesAsync,
  getPrayerTimesByLocation: () => getPrayerTimesByLocation,
  getPrayerTimesByLocationAsync: () => getPrayerTimesByLocationAsync,
  getQibla: () => getQibla,
  isFacingQibla: () => isFacingQibla,
  loadData: () => loadData,
  loadDataAsync: () => loadDataAsync,
  setBaseUrl: () => setBaseUrl,
  setDataLoader: () => setDataLoader
});
module.exports = __toCommonJS(index_exports);

// src/cities.ts
var KURDISTAN_CITIES = [
  { key: "Hawler", name: "Erbil", nameKurdish: "\u0647\u06D5\u0648\u0644\u06CE\u0631", lat: 36.19, lng: 44.01, radius: 0.4 },
  { key: "Slemani", name: "Sulaymaniyah", nameKurdish: "\u0633\u0644\u06CE\u0645\u0627\u0646\u06CC", lat: 35.56, lng: 45.44, radius: 0.4 },
  { key: "Duhok", name: "Duhok", nameKurdish: "\u062F\u0647\u06C6\u06A9", lat: 36.87, lng: 42.99, radius: 0.4 },
  { key: "Kirkuk", name: "Kirkuk", nameKurdish: "\u06A9\u06D5\u0631\u06A9\u0648\u06A9", lat: 35.47, lng: 44.39, radius: 0.4 },
  { key: "Zakho", name: "Zakho", nameKurdish: "\u0632\u0627\u062E\u06C6", lat: 37.15, lng: 42.68, radius: 0.3 },
  { key: "Halabja", name: "Halabja", nameKurdish: "\u06BE\u06D5\u06B5\u06D5\u0628\u062C\u06D5", lat: 35.18, lng: 45.98, radius: 0.3 },
  { key: "Koya", name: "Koya", nameKurdish: "\u06A9\u06C6\u06CC\u06D5", lat: 36.08, lng: 44.63, radius: 0.3 },
  { key: "Akre", name: "Akre", nameKurdish: "\u0626\u0627\u06A9\u0631\u06CE", lat: 36.74, lng: 43.88, radius: 0.3 },
  { key: "Qaladze", name: "Qaladze", nameKurdish: "\u0642\u06D5\u06B5\u0627\u062F\u0632\u06CE", lat: 36.18, lng: 45.11, radius: 0.3 },
  { key: "Darbandikhan", name: "Darbandikhan", nameKurdish: "\u062F\u06D5\u0631\u0628\u06D5\u0646\u062F\u06CC\u062E\u0627\u0646", lat: 35.11, lng: 45.69, radius: 0.3 },
  { key: "Soran", name: "Soran", nameKurdish: "\u0633\u06C6\u0631\u0627\u0646", lat: 36.65, lng: 44.54, radius: 0.3 },
  { key: "Choman", name: "Choman", nameKurdish: "\u0686\u06C6\u0645\u0627\u0646", lat: 36.63, lng: 44.89, radius: 0.3 },
  { key: "Penjwin", name: "Penjwin", nameKurdish: "\u067E\u06CE\u0646\u062C\u0648\u06CE\u0646", lat: 35.62, lng: 45.94, radius: 0.3 },
  { key: "Shaqlawa", name: "Shaqlawa", nameKurdish: "\u0634\u06D5\u0642\u06B5\u0627\u0648\u06D5", lat: 36.4, lng: 44.32, radius: 0.3 },
  { key: "Rawanduz", name: "Rawanduz", nameKurdish: "\u0695\u06D5\u0648\u0627\u0646\u062F\u0632", lat: 36.61, lng: 44.52, radius: 0.3 },
  { key: "Ranya", name: "Ranya", nameKurdish: "\u0695\u0627\u0646\u06CC\u06D5", lat: 36.25, lng: 44.88, radius: 0.3 },
  { key: "Kifri", name: "Kifri", nameKurdish: "\u06A9\u0641\u0631\u06CC", lat: 34.69, lng: 44.96, radius: 0.3 },
  { key: "Chamchamal", name: "Chamchamal", nameKurdish: "\u0686\u06D5\u0645\u0686\u06D5\u0645\u0627\u06B5", lat: 35.53, lng: 44.83, radius: 0.3 },
  { key: "Kalar", name: "Kalar", nameKurdish: "\u06A9\u06D5\u0644\u0627\u0631", lat: 34.63, lng: 45.32, radius: 0.3 },
  { key: "Baghdad", name: "Baghdad", nameKurdish: "\u0628\u06D5\u063A\u062F\u0627", lat: 33.31, lng: 44.36, radius: 0.5 },
  { key: "Mosul", name: "Mosul", nameKurdish: "\u0645\u0648\u0633\u06B5", lat: 36.34, lng: 43.13, radius: 0.5 },
  { key: "Basra", name: "Basra", nameKurdish: "\u0628\u06D5\u0633\u0631\u06D5", lat: 30.5, lng: 47.81, radius: 0.5 }
];
var CITY_DISPLAY = Object.fromEntries(
  KURDISTAN_CITIES.map((c) => [c.key, c.nameKurdish])
);
function getCityByCoordinates(lat, lng) {
  let closest = KURDISTAN_CITIES[KURDISTAN_CITIES.length - 1];
  let minDistance = Infinity;
  for (const city of KURDISTAN_CITIES) {
    const distance = Math.sqrt((lat - city.lat) ** 2 + (lng - city.lng) ** 2);
    if (distance <= city.radius && distance < minDistance) {
      minDistance = distance;
      closest = city;
    }
  }
  if (minDistance === Infinity) {
    return KURDISTAN_CITIES.find((c) => c.key === "Hawler");
  }
  return closest;
}
function getCityDisplayName(cityKey) {
  return CITY_DISPLAY[cityKey] || cityKey;
}
function getCity(cityKey) {
  return KURDISTAN_CITIES.find((c) => c.key === cityKey);
}

// src/loadData.ts
var import_node_fs = require("fs");
var import_node_path = require("path");
var import_node_url = require("url");
var import_meta = {};
var isBrowser = typeof window !== "undefined" || typeof self !== "undefined";
var customLoader = null;
var baseUrl = "https://unpkg.com/imanikurd-prayer@1.1.0/data/";
function setDataLoader(loader) {
  customLoader = loader;
}
function setBaseUrl(url) {
  baseUrl = url;
  if (!baseUrl.endsWith("/")) {
    baseUrl += "/";
  }
}
function getModuleDir() {
  if (isBrowser) return "";
  if (typeof __dirname !== "undefined") {
    return __dirname;
  }
  return (0, import_node_path.dirname)((0, import_node_url.fileURLToPath)(import_meta.url));
}
var moduleDir = isBrowser ? "" : getModuleDir();
var cache = /* @__PURE__ */ new Map();
function resolveDataPath(filename) {
  if (isBrowser) {
    throw new Error("imanikurd-prayer: Local file paths are not available in browser environments.");
  }
  const candidates = [
    (0, import_node_path.join)(moduleDir, "..", "data", filename),
    (0, import_node_path.join)(moduleDir, "data", filename),
    (0, import_node_path.join)(process.cwd(), "node_modules", "imanikurd-prayer", "data", filename)
  ];
  for (const candidate of candidates) {
    if ((0, import_node_fs.existsSync)(candidate)) return candidate;
  }
  throw new Error(
    `imanikurd-prayer: data file "${filename}" not found. Ensure the package is installed correctly.`
  );
}
function loadData(filename) {
  if (cache.has(filename)) {
    return cache.get(filename);
  }
  if (isBrowser) {
    throw new Error(
      `imanikurd-prayer: Synchronous data loading is not supported in browser environments. Use the asynchronous APIs.`
    );
  }
  const filePath = resolveDataPath(filename);
  const data = JSON.parse((0, import_node_fs.readFileSync)(filePath, "utf-8"));
  cache.set(filename, data);
  return data;
}
async function loadDataAsync(filename) {
  if (cache.has(filename)) {
    return cache.get(filename);
  }
  if (customLoader) {
    const data = await customLoader(filename);
    cache.set(filename, data);
    return data;
  }
  if (isBrowser) {
    const url = `${baseUrl}${filename}`;
    const response = await fetch(url);
    if (!response.ok) {
      throw new Error(`imanikurd-prayer: Failed to fetch data file from ${url} (status: ${response.status})`);
    }
    const data = await response.json();
    cache.set(filename, data);
    return data;
  }
  const filePath = resolveDataPath(filename);
  return new Promise((resolve, reject) => {
    (0, import_node_fs.readFile)(filePath, "utf-8", (err, dataStr) => {
      if (err) return reject(err);
      try {
        const data = JSON.parse(dataStr);
        cache.set(filename, data);
        resolve(data);
      } catch (parseErr) {
        reject(parseErr);
      }
    });
  });
}
function clearDataCache() {
  cache.clear();
}
function getDataFilePath(filename) {
  return resolveDataPath(filename);
}

// src/prayer.ts
var prayerCache = null;
function getPrayerData() {
  if (!prayerCache) {
    prayerCache = loadData("prayer_times.json");
  }
  return prayerCache;
}
async function getPrayerDataAsync() {
  if (!prayerCache) {
    prayerCache = await loadDataAsync("prayer_times.json");
  }
  return prayerCache;
}
function formatDateKey(date) {
  const month = String(date.getMonth() + 1).padStart(2, "0");
  const day = String(date.getDate()).padStart(2, "0");
  return `${month}-${day}`;
}
function toTimings(record) {
  return {
    Fajr: record.fajr,
    Sunrise: record.sunrise,
    Dhuhr: record.dhuhr,
    Asr: record.asr,
    Maghrib: record.maghrib,
    Isha: record.isha
  };
}
function parseTimeToMinutes(timeStr) {
  if (!timeStr) return 0;
  const cleaned = timeStr.split(" ")[0].trim();
  const [h, m] = cleaned.split(":").map(Number);
  if (isNaN(h) || isNaN(m)) return 0;
  return h * 60 + m;
}
function getPrayerCities() {
  return Object.keys(getPrayerData()).sort();
}
async function getPrayerCitiesAsync() {
  const data = await getPrayerDataAsync();
  return Object.keys(data).sort();
}
function getPrayerTimes(cityKey, date = /* @__PURE__ */ new Date()) {
  const data = getPrayerData();
  const cityRecords = data[cityKey] || data["Hawler"];
  if (!cityRecords) return null;
  const dateKey = formatDateKey(date);
  const record = cityRecords.find((r) => r.date === dateKey);
  return record ? toTimings(record) : null;
}
async function getPrayerTimesAsync(cityKey, date = /* @__PURE__ */ new Date()) {
  const data = await getPrayerDataAsync();
  const cityRecords = data[cityKey] || data["Hawler"];
  if (!cityRecords) return null;
  const dateKey = formatDateKey(date);
  const record = cityRecords.find((r) => r.date === dateKey);
  return record ? toTimings(record) : null;
}
function getDailyPrayerRecord(cityKey, date = /* @__PURE__ */ new Date()) {
  const data = getPrayerData();
  const cityRecords = data[cityKey] || data["Hawler"];
  if (!cityRecords) return null;
  const dateKey = formatDateKey(date);
  const record = cityRecords.find((r) => r.date === dateKey);
  if (!record) return null;
  return { ...record, timings: toTimings(record) };
}
async function getDailyPrayerRecordAsync(cityKey, date = /* @__PURE__ */ new Date()) {
  const data = await getPrayerDataAsync();
  const cityRecords = data[cityKey] || data["Hawler"];
  if (!cityRecords) return null;
  const dateKey = formatDateKey(date);
  const record = cityRecords.find((r) => r.date === dateKey);
  if (!record) return null;
  return { ...record, timings: toTimings(record) };
}
function getMonthlyPrayerTimes(cityKey, month, year = (/* @__PURE__ */ new Date()).getFullYear()) {
  const data = getPrayerData();
  const cityRecords = data[cityKey] || data["Hawler"] || [];
  return cityRecords.filter((record) => {
    const [mm] = record.date.split("-").map(Number);
    return mm === month;
  }).map((record) => {
    const [mm, dd] = record.date.split("-").map(Number);
    return {
      date: new Date(year, mm - 1, dd),
      day: dd,
      month: mm,
      timings: toTimings(record)
    };
  }).sort((a, b) => a.day - b.day);
}
async function getMonthlyPrayerTimesAsync(cityKey, month, year = (/* @__PURE__ */ new Date()).getFullYear()) {
  const data = await getPrayerDataAsync();
  const cityRecords = data[cityKey] || data["Hawler"] || [];
  return cityRecords.filter((record) => {
    const [mm] = record.date.split("-").map(Number);
    return mm === month;
  }).map((record) => {
    const [mm, dd] = record.date.split("-").map(Number);
    return {
      date: new Date(year, mm - 1, dd),
      day: dd,
      month: mm,
      timings: toTimings(record)
    };
  }).sort((a, b) => a.day - b.day);
}
function getCityFromCoordinates(lat, lng) {
  return getCityByCoordinates(lat, lng).key;
}
function getCityName(cityKey) {
  return getCityDisplayName(cityKey);
}
function getNextPrayer(cityKey, now = /* @__PURE__ */ new Date()) {
  const timings = getPrayerTimes(cityKey, now);
  if (!timings) return null;
  const prayers = [
    { name: "Fajr", time: timings.Fajr },
    { name: "Sunrise", time: timings.Sunrise },
    { name: "Dhuhr", time: timings.Dhuhr },
    { name: "Asr", time: timings.Asr },
    { name: "Maghrib", time: timings.Maghrib },
    { name: "Isha", time: timings.Isha }
  ];
  const currentMins = now.getHours() * 60 + now.getMinutes();
  for (const prayer of prayers) {
    const prayerMins = parseTimeToMinutes(prayer.time);
    if (prayerMins > currentMins) {
      const diff2 = prayerMins - currentMins;
      return {
        name: prayer.name,
        time: prayer.time,
        hoursUntil: Math.floor(diff2 / 60),
        minutesUntil: diff2 % 60
      };
    }
  }
  const fajrMins = parseTimeToMinutes(prayers[0].time);
  const diff = 24 * 60 - currentMins + fajrMins;
  return {
    name: prayers[0].name,
    time: prayers[0].time,
    hoursUntil: Math.floor(diff / 60),
    minutesUntil: diff % 60
  };
}
async function getNextPrayerAsync(cityKey, now = /* @__PURE__ */ new Date()) {
  const timings = await getPrayerTimesAsync(cityKey, now);
  if (!timings) return null;
  const prayers = [
    { name: "Fajr", time: timings.Fajr },
    { name: "Sunrise", time: timings.Sunrise },
    { name: "Dhuhr", time: timings.Dhuhr },
    { name: "Asr", time: timings.Asr },
    { name: "Maghrib", time: timings.Maghrib },
    { name: "Isha", time: timings.Isha }
  ];
  const currentMins = now.getHours() * 60 + now.getMinutes();
  for (const prayer of prayers) {
    const prayerMins = parseTimeToMinutes(prayer.time);
    if (prayerMins > currentMins) {
      const diff2 = prayerMins - currentMins;
      return {
        name: prayer.name,
        time: prayer.time,
        hoursUntil: Math.floor(diff2 / 60),
        minutesUntil: diff2 % 60
      };
    }
  }
  const fajrMins = parseTimeToMinutes(prayers[0].time);
  const diff = 24 * 60 - currentMins + fajrMins;
  return {
    name: prayers[0].name,
    time: prayers[0].time,
    hoursUntil: Math.floor(diff / 60),
    minutesUntil: diff % 60
  };
}
function getPrayerTimesByLocation(lat, lng, date = /* @__PURE__ */ new Date()) {
  const cityKey = getCityFromCoordinates(lat, lng);
  return getPrayerTimes(cityKey, date);
}
async function getPrayerTimesByLocationAsync(lat, lng, date = /* @__PURE__ */ new Date()) {
  const cityKey = getCityFromCoordinates(lat, lng);
  return getPrayerTimesAsync(cityKey, date);
}

// src/qibla.ts
var KAABA_COORDINATES = {
  lat: 21.4225,
  lng: 39.8262
};
function calculateQibla(latitude, longitude) {
  const phiK = KAABA_COORDINATES.lat * Math.PI / 180;
  const lambdaK = KAABA_COORDINATES.lng * Math.PI / 180;
  const phi = latitude * Math.PI / 180;
  const lambda = longitude * Math.PI / 180;
  const psi = 180 / Math.PI * Math.atan2(
    Math.sin(lambdaK - lambda),
    Math.cos(phi) * Math.tan(phiK) - Math.sin(phi) * Math.cos(lambdaK - lambda)
  );
  return Math.round((psi + 360) % 360);
}
function getQibla(from) {
  return {
    direction: calculateQibla(from.lat, from.lng),
    kaaba: KAABA_COORDINATES,
    from
  };
}
function isFacingQibla(heading, latitude, longitude, tolerance = 10) {
  const qibla = calculateQibla(latitude, longitude);
  const diff = Math.abs((heading - qibla + 180) % 360 - 180);
  return diff <= tolerance;
}
function getDistanceToKaaba(latitude, longitude) {
  const R = 6371;
  const dLat = (KAABA_COORDINATES.lat - latitude) * Math.PI / 180;
  const dLng = (KAABA_COORDINATES.lng - longitude) * Math.PI / 180;
  const a = Math.sin(dLat / 2) ** 2 + Math.cos(latitude * Math.PI / 180) * Math.cos(KAABA_COORDINATES.lat * Math.PI / 180) * Math.sin(dLng / 2) ** 2;
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return Math.round(R * c);
}
function getKaabaCoordinates() {
  return KAABA_COORDINATES;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  KAABA_COORDINATES,
  KURDISTAN_CITIES,
  calculateQibla,
  clearDataCache,
  getCity,
  getCityByCoordinates,
  getCityDisplayName,
  getCityFromCoordinates,
  getCityName,
  getDailyPrayerRecord,
  getDailyPrayerRecordAsync,
  getDataFilePath,
  getDistanceToKaaba,
  getKaabaCoordinates,
  getMonthlyPrayerTimes,
  getMonthlyPrayerTimesAsync,
  getNextPrayer,
  getNextPrayerAsync,
  getPrayerCities,
  getPrayerCitiesAsync,
  getPrayerTimes,
  getPrayerTimesAsync,
  getPrayerTimesByLocation,
  getPrayerTimesByLocationAsync,
  getQibla,
  isFacingQibla,
  loadData,
  loadDataAsync,
  setBaseUrl,
  setDataLoader
});
//# sourceMappingURL=index.cjs.map