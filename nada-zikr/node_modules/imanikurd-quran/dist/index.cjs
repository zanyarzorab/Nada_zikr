"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var index_exports = {};
__export(index_exports, {
  QARI_OPTIONS: () => QARI_OPTIONS,
  TAFSIR_OPTIONS: () => TAFSIR_OPTIONS,
  attachTafsir: () => attachTafsir,
  clearDataCache: () => clearDataCache,
  getAyah: () => getAyah,
  getAyahAsync: () => getAyahAsync,
  getDataFilePath: () => getDataFilePath,
  getFullQuran: () => getFullQuran,
  getFullQuranAsync: () => getFullQuranAsync,
  getJuzAyahs: () => getJuzAyahs,
  getJuzAyahsAsync: () => getJuzAyahsAsync,
  getPageAyahs: () => getPageAyahs,
  getPageAyahsAsync: () => getPageAyahsAsync,
  getQari: () => getQari,
  getQariOptions: () => getQariOptions,
  getQuranMetadata: () => getQuranMetadata,
  getQuranMetadataAsync: () => getQuranMetadataAsync,
  getRecitationUrl: () => getRecitationUrl,
  getSurah: () => getSurah,
  getSurahAsync: () => getSurahAsync,
  getSurahAyahs: () => getSurahAyahs,
  getSurahAyahsAsync: () => getSurahAyahsAsync,
  getSurahs: () => getSurahs,
  getSurahsAsync: () => getSurahsAsync,
  getTafsir: () => getTafsir,
  getTafsirAsync: () => getTafsirAsync,
  getTafsirForAyah: () => getTafsirForAyah,
  getTafsirForAyahAsync: () => getTafsirForAyahAsync,
  getTafsirForSurah: () => getTafsirForSurah,
  getTafsirForSurahAsync: () => getTafsirForSurahAsync,
  getTafsirOptions: () => getTafsirOptions,
  getTotalAyahCount: () => getTotalAyahCount,
  getTotalAyahCountAsync: () => getTotalAyahCountAsync,
  loadData: () => loadData,
  loadDataAsync: () => loadDataAsync,
  searchQuran: () => searchQuran,
  searchQuranAsync: () => searchQuranAsync,
  searchTafsir: () => searchTafsir,
  searchTafsirAsync: () => searchTafsirAsync,
  setBaseUrl: () => setBaseUrl,
  setDataLoader: () => setDataLoader
});
module.exports = __toCommonJS(index_exports);

// src/loadData.ts
var import_node_fs = require("fs");
var import_node_path = require("path");
var import_node_url = require("url");
var import_meta = {};
var isBrowser = typeof window !== "undefined" || typeof self !== "undefined";
var customLoader = null;
var baseUrl = "https://unpkg.com/imanikurd-quran@1.1.0/data/";
function setDataLoader(loader) {
  customLoader = loader;
}
function setBaseUrl(url) {
  baseUrl = url;
  if (!baseUrl.endsWith("/")) {
    baseUrl += "/";
  }
}
function getModuleDir() {
  if (isBrowser) return "";
  if (typeof __dirname !== "undefined") {
    return __dirname;
  }
  return (0, import_node_path.dirname)((0, import_node_url.fileURLToPath)(import_meta.url));
}
var moduleDir = isBrowser ? "" : getModuleDir();
var cache = /* @__PURE__ */ new Map();
function resolveDataPath(filename) {
  if (isBrowser) {
    throw new Error("imanikurd-quran: Local file paths are not available in browser environments.");
  }
  const candidates = [
    (0, import_node_path.join)(moduleDir, "..", "data", filename),
    (0, import_node_path.join)(moduleDir, "data", filename),
    (0, import_node_path.join)(process.cwd(), "node_modules", "imanikurd-quran", "data", filename)
  ];
  for (const candidate of candidates) {
    if ((0, import_node_fs.existsSync)(candidate)) return candidate;
  }
  throw new Error(
    `imanikurd-quran: data file "${filename}" not found. Ensure the package is installed correctly.`
  );
}
function loadData(filename) {
  if (cache.has(filename)) {
    return cache.get(filename);
  }
  if (isBrowser) {
    throw new Error(
      `imanikurd-quran: Synchronous data loading is not supported in browser environments. Use the asynchronous APIs.`
    );
  }
  const filePath = resolveDataPath(filename);
  const data = JSON.parse((0, import_node_fs.readFileSync)(filePath, "utf-8"));
  cache.set(filename, data);
  return data;
}
async function loadDataAsync(filename) {
  if (cache.has(filename)) {
    return cache.get(filename);
  }
  if (customLoader) {
    const data = await customLoader(filename);
    cache.set(filename, data);
    return data;
  }
  if (isBrowser) {
    const url = `${baseUrl}${filename}`;
    const response = await fetch(url);
    if (!response.ok) {
      throw new Error(`imanikurd-quran: Failed to fetch data file from ${url} (status: ${response.status})`);
    }
    const data = await response.json();
    cache.set(filename, data);
    return data;
  }
  const filePath = resolveDataPath(filename);
  return new Promise((resolve, reject) => {
    (0, import_node_fs.readFile)(filePath, "utf-8", (err, dataStr) => {
      if (err) return reject(err);
      try {
        const data = JSON.parse(dataStr);
        cache.set(filename, data);
        resolve(data);
      } catch (parseErr) {
        reject(parseErr);
      }
    });
  });
}
function clearDataCache() {
  cache.clear();
}
function getDataFilePath(filename) {
  return resolveDataPath(filename);
}

// src/quran.ts
var quranCache = null;
function getQuranData() {
  if (!quranCache) {
    quranCache = loadData("quran.json");
  }
  return quranCache;
}
async function getQuranDataAsync() {
  if (!quranCache) {
    quranCache = await loadDataAsync("quran.json");
  }
  return quranCache;
}
function getQuranMetadata() {
  return getQuranData().metadata;
}
async function getQuranMetadataAsync() {
  const data = await getQuranDataAsync();
  return data.metadata;
}
function getSurahs() {
  return getQuranData().surahs;
}
async function getSurahsAsync() {
  const data = await getQuranDataAsync();
  return data.surahs;
}
function getSurah(surahNumber) {
  return getQuranData().surahs.find((s) => s.number === surahNumber);
}
async function getSurahAsync(surahNumber) {
  const surahs = await getSurahsAsync();
  return surahs.find((s) => s.number === surahNumber);
}
function getSurahAyahs(surahNumber) {
  return getQuranData().ayahs.filter((a) => a.surah === surahNumber);
}
async function getSurahAyahsAsync(surahNumber) {
  const data = await getQuranDataAsync();
  return data.ayahs.filter((a) => a.surah === surahNumber);
}
function getAyah(surahNumber, ayahNumber) {
  return getQuranData().ayahs.find(
    (a) => a.surah === surahNumber && a.ayah === ayahNumber
  );
}
async function getAyahAsync(surahNumber, ayahNumber) {
  const data = await getQuranDataAsync();
  return data.ayahs.find(
    (a) => a.surah === surahNumber && a.ayah === ayahNumber
  );
}
function getFullQuran() {
  return getQuranData().ayahs;
}
async function getFullQuranAsync() {
  const data = await getQuranDataAsync();
  return data.ayahs;
}
function getJuzAyahs(juzNumber) {
  return getQuranData().ayahs.filter((a) => a.juz === juzNumber);
}
async function getJuzAyahsAsync(juzNumber) {
  const data = await getQuranDataAsync();
  return data.ayahs.filter((a) => a.juz === juzNumber);
}
function getPageAyahs(pageNumber) {
  return getQuranData().ayahs.filter((a) => a.page === pageNumber);
}
async function getPageAyahsAsync(pageNumber) {
  const data = await getQuranDataAsync();
  return data.ayahs.filter((a) => a.page === pageNumber);
}
function searchQuran(query) {
  const normalized = query.trim();
  if (!normalized) return [];
  return getQuranData().ayahs.filter((a) => a.text.includes(normalized));
}
async function searchQuranAsync(query) {
  const normalized = query.trim();
  if (!normalized) return [];
  const data = await getQuranDataAsync();
  return data.ayahs.filter((a) => a.text.includes(normalized));
}
function getTotalAyahCount() {
  return getQuranData().ayahs.length;
}
async function getTotalAyahCountAsync() {
  const data = await getQuranDataAsync();
  return data.ayahs.length;
}
function attachTafsir(ayahs, tafsirMap) {
  return ayahs.map((a) => ({
    ...a,
    tafsir: tafsirMap[a.ayah]
  }));
}

// src/tafsir.ts
var TAFSIR_OPTIONS = [
  { id: "rebar", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u0695\u06CE\u0628\u06D5\u0631", file: "tafsir_rebar.json" },
  { id: "asan", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u0626\u0627\u0633\u0627\u0646", file: "tafsir_asan.json" },
  { id: "puxta", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u067E\u0648\u062E\u062A\u06D5", file: "tafsir_puxta.json" },
  { id: "raman", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u0695\u0627\u0645\u0627\u0646", file: "tafsir_raman.json" },
  { id: "hazhar", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u0647\u06D5\u0698\u0627\u0631 (\u0645\u0627\u0645\u06C6\u0633\u062A\u0627 \u0647\u06D5\u0698\u0627\u0631)", file: "tafsir_hazhar.json" },
  { id: "zhin", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u0698\u06CC\u0627\u0646", file: "tafsir_zhin.json" },
  { id: "sanahi", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u0633\u06D5\u0646\u0627\u0647\u06CC", file: "tafsir_sanahi.json" },
  { id: "maisar", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u0645\u0648\u06CC\u06D5\u0633\u06D5\u0631", file: "tafsir_maisar.json" },
  { id: "tawhid", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u062A\u06D5\u0648\u062D\u06CC\u062F\u06CC", file: "tafsir_tawhid.json" },
  { id: "roshn", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u0695\u06C6\u0634\u0646", file: "tafsir_roshn.json" },
  { id: "runahi", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u0695\u0648\u0646\u0627\u0647\u06CC", file: "tafsir_runahi.json" },
  { id: "mokhtasar", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u0645\u0648\u062E\u062A\u06D5\u0633\u06D5\u0631", file: "tafsir_mokhtasar.json" },
  { id: "krd", name: "\u062A\u06D5\u0641\u0633\u06CC\u0631\u06CC \u06A9\u0648\u0631\u062F\u06CC", file: "tafsir_krd.json" }
];
var tafsirCache = /* @__PURE__ */ new Map();
function normalizeEntry(item) {
  return {
    s: typeof item.s === "string" ? parseInt(item.s, 10) : item.s,
    a: item.a,
    t: item.t
  };
}
function getTafsirFile(tafsirId) {
  const option = TAFSIR_OPTIONS.find((t) => t.id === tafsirId);
  if (!option) {
    throw new Error(
      `Unknown tafsir id "${tafsirId}". Valid ids: ${TAFSIR_OPTIONS.map((t) => t.id).join(", ")}`
    );
  }
  return option.file;
}
function loadTafsirRaw(tafsirId) {
  if (tafsirCache.has(tafsirId)) {
    return tafsirCache.get(tafsirId);
  }
  const file = getTafsirFile(tafsirId);
  const raw = loadData(file);
  const entries = raw.map(normalizeEntry);
  tafsirCache.set(tafsirId, entries);
  return entries;
}
async function loadTafsirRawAsync(tafsirId) {
  if (tafsirCache.has(tafsirId)) {
    return tafsirCache.get(tafsirId);
  }
  const file = getTafsirFile(tafsirId);
  const raw = await loadDataAsync(file);
  const entries = raw.map(normalizeEntry);
  tafsirCache.set(tafsirId, entries);
  return entries;
}
function getTafsirOptions() {
  return TAFSIR_OPTIONS;
}
function getTafsir(tafsirId) {
  return loadTafsirRaw(tafsirId);
}
async function getTafsirAsync(tafsirId) {
  return loadTafsirRawAsync(tafsirId);
}
function getTafsirForAyah(tafsirId, surahNumber, ayahNumber) {
  const entries = loadTafsirRaw(tafsirId);
  const entry = entries.find((e) => e.s === surahNumber && e.a === ayahNumber);
  return entry?.t;
}
async function getTafsirForAyahAsync(tafsirId, surahNumber, ayahNumber) {
  const entries = await loadTafsirRawAsync(tafsirId);
  const entry = entries.find((e) => e.s === surahNumber && e.a === ayahNumber);
  return entry?.t;
}
function getTafsirForSurah(tafsirId, surahNumber) {
  const entries = loadTafsirRaw(tafsirId);
  const result = {};
  for (const entry of entries) {
    if (entry.s === surahNumber) {
      result[entry.a] = entry.t;
    }
  }
  return result;
}
async function getTafsirForSurahAsync(tafsirId, surahNumber) {
  const entries = await loadTafsirRawAsync(tafsirId);
  const result = {};
  for (const entry of entries) {
    if (entry.s === surahNumber) {
      result[entry.a] = entry.t;
    }
  }
  return result;
}
function searchTafsir(tafsirId, query) {
  const normalized = query.trim();
  if (!normalized) return [];
  return loadTafsirRaw(tafsirId).filter((e) => e.t.includes(normalized));
}
async function searchTafsirAsync(tafsirId, query) {
  const normalized = query.trim();
  if (!normalized) return [];
  const entries = await loadTafsirRawAsync(tafsirId);
  return entries.filter((e) => e.t.includes(normalized));
}

// src/audio.ts
var QARI_OPTIONS = [
  {
    id: "peshawa",
    name: "\u067E\u06CE\u0634\u06D5\u0648\u0627 \u0642\u0627\u062F\u0631 (\u06A9\u0648\u0631\u062F\u06CC)",
    nameAr: "Peshawa Qadir",
    getUrl: (surahNum) => {
      const padded = surahNum.toString().padStart(3, "0");
      return `https://media.githubusercontent.com/media/w-coding/Peshawa-Qadir/main/mp3%20files/${padded}.mp3`;
    }
  },
  {
    id: "alafasy",
    name: "\u0645\u0634\u0627\u0631\u06CC \u0627\u0644\u0639\u0641\u0627\u0633\u06CC",
    nameAr: "Mishary Alafasy",
    getUrl: (surahNum) => {
      const padded = surahNum.toString().padStart(3, "0");
      return `https://server8.mp3quran.net/afs/${padded}.mp3`;
    }
  },
  {
    id: "husary",
    name: "\u0645\u062D\u0645\u0648\u062F \u062E\u0644\u06CC\u0644 \u0627\u0644\u062D\u0635\u0631\u06CC",
    nameAr: "Mahmoud Al-Husary",
    getUrl: (surahNum) => {
      const padded = surahNum.toString().padStart(3, "0");
      return `https://server7.mp3quran.net/husr/${padded}.mp3`;
    }
  },
  {
    id: "minshawi",
    name: "\u0645\u062D\u0645\u062F \u0635\u062F\u06CC\u0642 \u0627\u0644\u0645\u0646\u0634\u0627\u0648\u06CC",
    nameAr: "Al-Minshawi",
    getUrl: (surahNum) => {
      const padded = surahNum.toString().padStart(3, "0");
      return `https://server10.mp3quran.net/minsh/${padded}.mp3`;
    }
  },
  {
    id: "abdulbasit",
    name: "\u0639\u0628\u062F\u0627\u0644\u0628\u0627\u0633\u0637 \u0639\u0628\u062F\u0627\u0644\u0635\u0645\u062F",
    nameAr: "Abdul Basit",
    getUrl: (surahNum) => {
      const padded = surahNum.toString().padStart(3, "0");
      return `https://server7.mp3quran.net/basit/${padded}.mp3`;
    }
  },
  {
    id: "sudais",
    name: "\u0639\u0628\u062F\u0627\u0644\u0631\u062D\u0645\u0646 \u0627\u0644\u0633\u062F\u06CC\u0633",
    nameAr: "Abdulrahman Al-Sudais",
    getUrl: (surahNum) => {
      const padded = surahNum.toString().padStart(3, "0");
      return `https://server11.mp3quran.net/sds/${padded}.mp3`;
    }
  },
  {
    id: "shuraim",
    name: "\u0633\u0639\u0648\u062F \u0627\u0644\u0634\u0631\u06CC\u0645",
    nameAr: "Saud Al-Shuraim",
    getUrl: (surahNum) => {
      const padded = surahNum.toString().padStart(3, "0");
      return `https://server7.mp3quran.net/shur/${padded}.mp3`;
    }
  },
  {
    id: "maher",
    name: "\u0645\u0627\u0647\u0631 \u0627\u0644\u0645\u0639\u06CC\u0642\u0644\u06CC",
    nameAr: "Maher Al-Muaiqly",
    getUrl: (surahNum) => {
      const padded = surahNum.toString().padStart(3, "0");
      return `https://server12.mp3quran.net/maher/${padded}.mp3`;
    }
  },
  {
    id: "ghamdi",
    name: "\u0633\u0639\u062F \u0627\u0644\u063A\u0627\u0645\u062F\u06CC",
    nameAr: "Saad Al-Ghamdi",
    getUrl: (surahNum) => {
      const padded = surahNum.toString().padStart(3, "0");
      return `https://server7.mp3quran.net/s_gmd/${padded}.mp3`;
    }
  },
  {
    id: "ajamy",
    name: "\u0623\u062D\u0645\u062F \u0627\u0644\u0639\u062C\u0645\u06CC",
    nameAr: "Ahmed Al-Ajmi",
    getUrl: (surahNum) => {
      const padded = surahNum.toString().padStart(3, "0");
      return `https://server10.mp3quran.net/ajm/${padded}.mp3`;
    }
  }
];
function getQariOptions() {
  return QARI_OPTIONS;
}
function getRecitationUrl(qariId, surahNumber) {
  const qari = QARI_OPTIONS.find((q) => q.id === qariId);
  return qari?.getUrl(surahNumber);
}
function getQari(qariId) {
  return QARI_OPTIONS.find((q) => q.id === qariId);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  QARI_OPTIONS,
  TAFSIR_OPTIONS,
  attachTafsir,
  clearDataCache,
  getAyah,
  getAyahAsync,
  getDataFilePath,
  getFullQuran,
  getFullQuranAsync,
  getJuzAyahs,
  getJuzAyahsAsync,
  getPageAyahs,
  getPageAyahsAsync,
  getQari,
  getQariOptions,
  getQuranMetadata,
  getQuranMetadataAsync,
  getRecitationUrl,
  getSurah,
  getSurahAsync,
  getSurahAyahs,
  getSurahAyahsAsync,
  getSurahs,
  getSurahsAsync,
  getTafsir,
  getTafsirAsync,
  getTafsirForAyah,
  getTafsirForAyahAsync,
  getTafsirForSurah,
  getTafsirForSurahAsync,
  getTafsirOptions,
  getTotalAyahCount,
  getTotalAyahCountAsync,
  loadData,
  loadDataAsync,
  searchQuran,
  searchQuranAsync,
  searchTafsir,
  searchTafsirAsync,
  setBaseUrl,
  setDataLoader
});
//# sourceMappingURL=index.cjs.map